version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: dto_revisao
    restart: always
    environment:
      MYSQL_DATABASE: dto_revisao
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"